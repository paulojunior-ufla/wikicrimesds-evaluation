import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appWikiCrimesDS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
